/**
 * API Configuration
 * 
 * This file exports the API configuration constants.
 * Import BASE_URL for constructing file URLs (avatars, documents).
 * Import API_URL for direct API endpoint access (if needed).
 * Use the default export 'api' instance for all API calls.
 */

export { BASE_URL, API_URL, default as api } from '../utils/axios';

